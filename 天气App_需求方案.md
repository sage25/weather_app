# 天气App需求方案（老年人优先）

## 1. 项目背景
- 目标平台：Android（Flutter开发）
- 发布方式：不发布应用商店，仅家庭内部使用
- 核心用户：老年人（主），年轻人（次）
- 设计目标：消除阅读障碍、尽量少点击和少滑动、极简大字体

## 2. 产品目标
- 用户打开App后能快速看懂天气，不需要学习复杂操作
- 只展示今天和明天，避免信息过载
- 首次完成经纬度设置后，后续打开自动刷新

## 3. 用户与使用定位
- 平时只看一个地区（由经纬度确定）
- 允许后续修改经纬度（主要给年轻人使用）
- “设置/修改经纬度”入口不需要做得特别显眼，也不需要超大按钮

## 4. 核心流程

### 4.1 首次使用
1. 打开App进入“初始化设置”页面
2. 由家人填写纬度（latitude）和经度（longitude）
3. 点击“确认地区”按钮完成绑定
4. 保存经纬度后进入天气主页

经纬度输入规则：
- latitude范围：-90 到 90
- longitude范围：-180 到 180
- 支持小数（如29.56、106.55）
- 输入非法时给出明确错误提示，不允许提交

### 4.2 非首次使用
1. 打开App直接进入天气主页
2. 自动刷新天气数据
3. 刷新成功后展示最新今天/明天信息

### 4.3 修改经纬度
1. 在主页提供“设置”图标入口（不强调视觉优先级）
2. 点击设置后进入“经纬度设置”页面
3. 修改成功后覆盖旧经纬度并刷新数据

## 5. 信息架构与页面结构

### 5.1 页面总结构
- 单页纵向滚动
- 第一屏：今天（占满一整屏）
- 向下滑动：明天（占满一整屏）
- 今天与明天背景色不同，便于快速分辨
- 主页右上角提供“设置”图标，用于修改经纬度

### 5.2 每日内容结构（今天和明天一致）
每日分为三栏：
1. 第一栏（上午）：00:00-12:00 天气变化 + 温度变化
2. 第二栏（下午）：12:00-24:00 天气变化 + 温度变化
3. 第三栏（4行x6小时，总览+细分两层）：
   - 第一层（6小时总览）：
     - 00:00-06:00 总体天气 + 温度范围
     - 06:00-12:00 总体天气 + 温度范围
     - 12:00-18:00 总体天气 + 温度范围
     - 18:00-24:00 总体天气 + 温度范围
   - 第二层（每2小时一组，显示在对应6小时段下方）：
     - 00:00-02:00、02:00-04:00、04:00-06:00
     - 06:00-08:00、08:00-10:00、10:00-12:00
     - 12:00-14:00、14:00-16:00、16:00-18:00
     - 18:00-20:00、20:00-22:00、22:00-24:00
   - 每个2小时分组都要显示：天气类型 + 温度
   - 示例（单个6小时段）：
     - 00:00-06:00 多云 18-22℃
     - 00:00-02:00 多云 18℃，02:00-04:00 阴 19℃，04:00-06:00 小雨 20℃

## 6. 显示规范

### 6.1 文案风格
- 极简文案：仅天气类型 + 温度
- 示例：晴、多云、小雨；18-24℃
- 不强制显示风力、湿度、体感温度等复杂信息

### 6.2 温度单位
- 固定使用摄氏度（℃）

### 6.3 可读性与交互
- 大字号、清晰层级、留白充足
- 操作尽量少：主要行为为“打开即看”和“向下滑到明天”
- 仅竖屏，不支持横屏

## 7. 刷新与异常处理

### 7.1 刷新策略
- 每次打开App自动刷新一次
- 暂不要求后台定时刷新

### 7.2 网络/接口失败
- 显示空白错误页
- 错误页保留一个“重试”按钮

## 8. 兼容性与技术约束
- Android版本范围：Android 10-15
- 技术框架：Flutter
- 本地存储：至少保存latitude、longitude
- 网络请求：固定使用Open-Meteo API
- API格式：
  - https://api.open-meteo.com/v1/forecast?latitude={latitude}&longitude={longitude}&hourly=temperature_2m,weathercode,relative_humidity_2m,apparent_temperature,precipitation,cloudcover&timezone=Asia%2FShanghai&forecast_days=2
- 当前默认示例：
  - https://api.open-meteo.com/v1/forecast?latitude=29.56&longitude=106.55&hourly=temperature_2m,weathercode,relative_humidity_2m,apparent_temperature,precipitation,cloudcover&timezone=Asia%2FShanghai&forecast_days=2
- 需支持后续修改latitude和longitude并立即生效

## 9. 天气数据要求（用于实现）
- 需要可覆盖“今天+明天”的逐时天气数据
- 实现端需把逐时数据聚合成：
  - 上午（00-12）变化信息
  - 下午（12-24）变化信息
  - 4个6小时总览信息
  - 每个6小时段内再拆分为3个2小时分组信息（即每天12个2小时分组）

## 10. 验收标准
1. 首次使用必须先填写经纬度，确认后才能进主页
2. 非首次打开自动刷新并显示天气
3. 主页只显示今天和明天两块内容
4. 今天整屏显示，向下滑到明天
5. 两天背景色明显不同
6. 每天都含上午栏、下午栏、4行6小时栏
7. 每个6小时栏内都包含3个2小时分组的具体天气与温度
8. 温度统一为℃
9. 网络失败显示空白错误页，并能点击“重试”
10. 主页有设置图标，可修改经纬度，入口无需显眼放大
11. 在Android 10-15设备可正常使用
12. 修改经纬度后，下一次请求URL中的latitude和longitude必须使用新值

## 11. 非目标（当前不做）
- 应用商店发布相关能力
- 语音播报
- 高对比度模式切换
- 多地区同时关注
- 复杂气象指标面板
